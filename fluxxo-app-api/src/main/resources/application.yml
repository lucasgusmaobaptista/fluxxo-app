spring:
  application:
    name: fluxxo-app-api
  datasource:
    url: jdbc:postgresql://localhost:5432/fluxxoappdb
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  servlet:
    context-path: /api/v1
